version: '3.8'

services:
  web-nginx:
    image: nginx:alpine
    container_name: react_nginx_bai13
    volumes:
      - ./build:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8084:80"